<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Practica06LayoutsDSM104" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Puedes agregar elementos adicionales a este tema base si lo deseas -->
    </style>

    <style name="Theme.Practica06LayoutsDSM104" parent="Base.Theme.Practica06LayoutsDSM104" />

    <!-- Estilo base para los botones de la calculadora -->
    <style name="AppTheme.BotonCalculadora" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">36dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Estilo azul para los botones de la calculadora -->
    <style name="AppTheme.BotonCalculadora.Azul" parent="AppTheme.BotonCalculadora">
        <item name="android:background">#00537D</item>
    </style>

    <!-- Estilo rojo para los botones de la calculadora -->
    <style name="AppTheme.BotonCalculadora.Rojo" parent="AppTheme.BotonCalculadora">
        <item name="android:background">#EA4D39</item>
    </style>
</resources>
